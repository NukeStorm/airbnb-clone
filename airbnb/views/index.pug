extends layout
block content
  //include ./searchbar.pug
  header
    include ./navbar.pug
  //-
    if session === null
      h1= title
      form.modal-content.animate(action='/login' method='POST')
        .container
          label(for='id')
            b ID
          input(type='text' placeholder='ID 입력' name='id' required='')
          label(for='pw')
            b Password
          input(type='password' placeholder='비밀번호 입력' name='pw' required='')
          button(type='submit') Login
          label
            input(type='checkbox' checked='checked' name='remember')
            |  로그인 유지
        .container(style='background-color:#f1f1f1')
    else
      button(type='button' onclick="location.href='logout'"  ) 로그아웃
      
  //
 
  section(style='background-color:white;height:1000px;margin-top:100px')
    div(style='text-align:center')
      img(src='/images/topimage.jpg')
    div.secondview 
      div(style='max-width:33.333%')
        img(src='/images/second1.jpg')
      div(style='max-width:33.333%')
        img(src='/images/second2.jpg')
      div(style='max-width:33.333%')
        img(src='/images/second3.jpg')

  section(style='background-color:black;height:1000px')
    h1 섹션2 공사중
  footer(style='background-color:white;height:1000px')
    include ./footer.pug

  script.

    var setCookie = function(name, value, day) {
      var date = new Date();
      date.setTime(date.getTime() + day * 60 * 60 * 24 * 1000);
      document.cookie = name + '=' + value + ';expires=' + date.toUTCString() + ';path=/';
    };

    var getCookie = function(name) {
    var value = document.cookie.match('(^|;) ?' + name + '=([^;]*)(;|$)');
    return value? value[2] : null;
    };

    var deleteCookie = function(name) {
      console.log("del cookie");
      setCookie(name, '',-1);
    }





